# Repository

This repository currently contains:

- `Zadacha.txt` – a task description.
- `dump.zip` / `dump/dump.cs` – a C# dump.

## Safety note

The task description requests functionality that would enable unauthorized code injection into a third‑party Android application and includes cheat/anti‑debugging requirements. Implementing that is not something we can provide here.

If you want a legitimate Android Studio (NDK/CMake) sample project for **your own app/process** (e.g., plugin architecture, in‑app overlay UI, integrity checks for your own modules), open a new ticket with:

- your application package name,
- the exact functional requirements that stay within your app’s process,
- desired minSdk/targetSdk.
